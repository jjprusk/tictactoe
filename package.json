{
  "name": "tictactoe",
  "private": true,
  "version": "0.1.0",
  "packageManager": "npm@11.3.0",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "start:server": "npm --workspace server run dev",
    "start:client": "npm --workspace client run dev",
    "prepare": "husky",
    "test": "npm --workspace server run test",
    "test:watch": "npm --workspace server run test:watch",
    "test:cov": "npm --workspace server run test:cov",
    "load:socket": "artillery run server/load/socketio.yml"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "artillery": "^2.0.10",
    "husky": "^9.1.7"
  }
}
